// Test setup file
import '@jest/globals';

beforeAll(async () => {
  // Setup test database or mock dependencies
});

afterAll(async () => {
  // Cleanup test database or mock dependencies
});

beforeEach(() => {
  // Reset mocks before each test
  jest.clearAllMocks();
});